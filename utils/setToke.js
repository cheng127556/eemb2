/*方法说明
 *@method verifyMobile
 *@param{参数类型为数字}}参数名：MobileNum 参数说明：传入一个数字验证是不是正确的手机号格式
 *@return {false} 返回值说明:如果不是正确的手机号格式，就返回false终止函数
*/
var token = require('./token.js');
//_this, obj.myRequest, obj.data, obj.func1, obj.func2, obj.func3, obj.info


module.exports = {
  setObj: setObj,
}